# REST API 응답

## 토큰 재발급 HTTP STATUS
 
* 토큰 재발급 http status 값을 가지고 토큰 재발급 성공 여부를 판단함
  http status 값을 사용자가 없을땐 404, 이전에 발급받은 토큰이 아닐때 403으로 주면 클라이언트 입장에선 단순히 http status 값만 가지고 토큰 재발급 성공 여부를 판단하기 어려움
  그러니 토큰 재발급 실패한 경우는 이유가 뭐가 됐던 모두 401로 통일하는게 좋음
